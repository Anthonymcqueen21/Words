String = An ordered sequence of characters.
Array = A data structure that allows a sequence of values.
Character = A single letter or symbol
Pointer = Stores the address of an object
Scanf = Scans input that matches format specifiers 
Print = Function used to generate an output.
Argument = A piece of data that is passed into a function program.
Integer = Declare a whole number.
(Int) = Used to declare an integer variable.
Float = Used to declare a floating point variable.
